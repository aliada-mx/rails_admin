= append(:body_class){ ' two-columns ' }
- if user_signed_in?
  = append(:title){ " - #{@user.name}" }

= extends :application do
  .container
    .row
      = render partial: 'common/header'

    .row
      = area :navigation

    .row 
      = render partial: 'common/messages'

    .row
      = yield


= render partial: "common/footer"
