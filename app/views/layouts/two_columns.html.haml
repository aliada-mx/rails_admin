= extends :application do
  .container.two-columns
    .row
      = render partial: 'common/header'

    .row
      %nav
        = area :navigation

    .row
      %main
        = yield

      %aside
        = area :sidebar
