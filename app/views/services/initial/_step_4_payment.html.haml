%section.step_4_payment{data: {bind: 'visible: current_step() == 4'}}
  = initial_service_form.collection_radio_buttons :payment_method_id, PaymentMethod.all, :id, :name
  = render partial: 'common/conekta_form'
